Match Host * Exec "gpg-connect-agent UPDATESTARTUPTTY /bye"

Match Host * exec "curl -I %h 2>/dev/null | grep -Po 'Server: cloudflare'"
    ProxyCommand /usr/bin/cloudflared access ssh --hostname %h

