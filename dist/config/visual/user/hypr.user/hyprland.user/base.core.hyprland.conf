# depends: base.{theme,var,env}.hyprland.config

# DEPENDENCIES

source = base.theme.hyprland.conf
source = base.var.hyprland.conf
source = base.env.hyprland.conf


# DISPLAY

source = base.display.hyprland.conf

# ACTIONS

source = base.action.hyprland.conf

# INPUT

source = base.input.hyprland.conf

# KEYBINDS

source = base.bind.hyprland.conf

# RUN

source = base.run.hyprland.conf

# WINDOW RULES

source = base.window.hyprland.conf

# GENERAL SETTINGS

source = base.general.hyprland.conf

# GRAPHIC SETTINGS

source = base.graphic.hyprland.conf

# HYPRLAND CORE RULES

master {
    new_on_top        = true
}

misc {
    disable_hyprland_logo    = true
    disable_splash_rendering = true

    vfr = true
}

debug {
    damage_tracking    = 2
    enable_stdout_logs = false
}
